create database Cadastro_login;
use Cadastro_login;


create table Dados(
id int primary key not null auto_increment,
nome varchar(60),
email varchar(60),
telefone varchar(11),
sexo char(1));

select * from Dados;
